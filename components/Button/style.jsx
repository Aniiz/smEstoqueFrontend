import { StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  button: {
    alignItems: "center",
    justifyContent: "center",
    padding: 10,
  },
  btnText: {
    fontSize: 20,
    color: 'white',
    fontWeight: "bold"
  }
});