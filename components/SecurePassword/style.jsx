import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  secureContainer: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 30,
  },

  descriptionSecure: {
    marginRight: 15
  }
});