import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  secureContainer: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    margin: 10
  },

  descriptionSecure: {
    marginRight: 15
  }
});